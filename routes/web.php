<?php

use App\Http\Controllers\BookController;
use App\Http\Controllers\UserController;
use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider and all of them will
| be assigned to the "web" middleware group. Make something great!
|
*/

//Route::get('register', [UserController::class, 'showRegister'])->name('showRegister');
//Route::post('register/submit', [UserController::class, 'register'])->name('register');
//
//Route::get('/', [UserController::class, 'showLogin'])->name('showLogin');
//Route::post('/submit', [UserController::class, 'login'])->name('login');
//
//Route::get('/admin-dashboard', [BookController::class, 'showAdminList'])->name('showAdminList')->middleware(['auth'])->middleware(['accType']);
//Route::post('/admin-dashboard', [BookController::class, 'addBook'])->name('addBook')->middleware(['auth'])->middleware(['accType']);


